<%= render partial: 'shared/admin_links', locals: {path: "/admin"} %>

<body>
  <div class="row">
    <h2 class='col-12'>Invoices</h2>
  </div>
  <% @invoices.each do |i| %>
    <li><%= link_to "Invoice ##{i.id}", admin_invoice_path(i) %></li>
  <% end %>
</body>
