<%= render partial: 'shared/merchant_links', locals: {path: "/merchant/#{@merchant.id}"} %>

<h2><%= number_to_percentage(@bulk_discount.percent_off, :precision => 0) %> Discount</h2>
  <p>Quantity Threshold: <%= @bulk_discount.thresholds %></p>
  <p>Status: <%= @bulk_discount.status %></p>
<%= link_to "Edit Discount", edit_merchant_bulk_discount_path(@merchant, @bulk_discount), method: :get %>
